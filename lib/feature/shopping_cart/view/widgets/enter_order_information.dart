import 'package:flutter/material.dart';

class EnterOrderInformation extends StatelessWidget {
  const EnterOrderInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}
